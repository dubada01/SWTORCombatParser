<UserControl x:Class="SWTORCombatParser.Views.Battle_Review.EventHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SWTORCombatParser.Views.Battle_Review"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView x:Name="EventsList" ItemsSource="{Binding LogsToDisplay}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width=".15*"/>
                            <ColumnDefinition Width=".33*"/>
                            <ColumnDefinition Width=".33*"/>
                            <ColumnDefinition Width=".33*"/>
                            <ColumnDefinition Width=".15*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock FontSize="12" Text = "{Binding SecondsSinceCombatStart}"></TextBlock>
                        <TextBlock FontSize="12"  Grid.Column = "1" Text = "{Binding Source.Name}"></TextBlock>
                        <TextBlock FontSize="12" Grid.Column = "2" Text="{Binding Target.Name}"></TextBlock>
                        <TextBlock FontSize="12" Grid.Column = "3" Text="{Binding Ability}"></TextBlock>
                        <TextBlock FontSize="12" Grid.Column = "4" Text="{Binding Value.DisplayValue}"></TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
