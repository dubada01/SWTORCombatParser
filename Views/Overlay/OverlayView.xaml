<UserControl x:Class="SWTORCombatParser.Views.OverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SWTORCombatParser.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ItemsControl ItemsSource="{Binding AvailableOverlayTypes}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Viewbox HorizontalAlignment="Center" Margin="0,25,0,0" MaxWidth="350">
                        <Button Padding="5" CommandParameter="{Binding .}" Width="350" Command="{Binding DataContext.GenerateOverlay, RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                            <Button.Content>
                                <TextBlock Text="{Binding ., StringFormat= 'Show \{0\} overlay'}"></TextBlock>
                            </Button.Content>
                        </Button>
                    </Viewbox>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Viewbox HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10" MaxWidth="200">
            <Button Content="Toggle Overlay Lock" Padding="5"  Command="{Binding ToggleOverlayLockCommand}"></Button>
        </Viewbox>
    </Grid>
</UserControl>
