<UserControl x:Class="SWTORCombatParser.Views.OverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SWTORCombatParser.Views"
             xmlns:local1="clr-namespace:SWTORCombatParser.ViewModels.Overlays"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Available Overlays" HorizontalAlignment="Center"></TextBlock>
        <ItemsControl Margin="0,20,0,0" ItemsSource="{Binding AvailableOverlayTypes}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                        <Button Padding="5" CommandParameter="{Binding .}" Height="50" Width="200" Command="{Binding DataContext.GenerateOverlay, RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                            <Button.Content>
                                <Viewbox MaxHeight="25">
                                    <TextBlock Text="{Binding .}"></TextBlock>
                                </Viewbox>
                            </Button.Content>
                        </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="2"></UniformGrid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        <TextBlock Grid.Column="1" Text="Available Alerts" HorizontalAlignment="Center"></TextBlock>
        <ItemsControl Grid.Column="1" Margin="0,20,0,0" ItemsSource="{Binding AvailableAlerts}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local1:AlertOption/>
                </DataTemplate>
                
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="2"></UniformGrid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <Viewbox Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10" MaxWidth="200">
            <Button Content="Toggle Overlay Lock" Padding="5"  Command="{Binding ToggleOverlayLockCommand}"></Button>
        </Viewbox>
    </Grid>
</UserControl>
